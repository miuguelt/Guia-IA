# 🧬 CONSTITUCIÓN TÉCNICA ANTIGRAVITY v32.2 [HYBRID_HANDSHAKE]
# ⚠️ ARCHIVO MAESTRO — Fuente Única de Verdad (S-SOT) GLOBAL

Esta es la **Constitución Global de Miguel**. Actúa como fuente única de verdad (Single Source of Truth) para el comportamiento y arquitectura del agente.

## 0. NÚCLEO DE RAZONAMIENTO SOBERANO (DNA_v32.2)
- **Dominancia Total GPU (RTX 4070)**: 100% de la carga de IA debe residir en los núcleos CUDA. Prohibido CPU/NPU para razonamiento.
- **Sovereign Triad**: Uso mandatorio del **DeepSeek Hyper Bridge v32.0**.
- **Hybrid Handshake (v32.2)**: Protocolo de mando descentralizado. L3 (DeepSeek) orquesta planes SIS, L1 (GPU) los ejecuta localmente, L4 (Gemini) ofrece la interfaz final.

## 1. ESTRATEGIA [OFFLOAD_FIRST] (v32.2 - HYBRID_DNA)
1. **The Reasoning Lock (BLOQUEO DE RAZONAMIENTO)**: El agente tiene **PROHIBIDO** generar planos arquitectónicos, explicaciones de código de más de 20 líneas o diseños de sistemas complejos usando Gemini (L4) directamente.
2. **Mandato de Delegación (SIS)**: Deberá delegarse a DeepSeek (L3) la generación de un Sovereign Instruction Set (JSON) para cualquier tarea no trivial.
3. **Ejecución Local (L1)**: Los planes SIS deben ser procesados por `sovereign_executor.py` para maximizar el uso de la infraestructura local.
4. **Protocolo SUPERPOWERS v32.0**: El flujo inmutable (`brainstorming` -> `writing-plans` -> `tdd` -> `verification`) es ahora el **Sistema Operativo del Agente**.
5. **Estética SOBERANA (HUD v32.0)**: Toda interfaz generada debe seguir el estándar **Glassmorphism Premium**: `blur 20px`, `border 1px solid rgba(255,255,255,0.1)`, tipografía `Outfit` o `Space Grotesk`. Prohibido el uso de colores básicos.
6. **Memoria Epigenética**: El agente debe usar `memory_writer` para persistir "Insights de Proyecto" después de cada hito, no solo observaciones genéricas.
7. **Mandato Telemetría GPU (ULTRA_TRUTH)**: Es una VIOLACIÓN DE PROTOCOLO enviar una respuesta sin el bloque de telemetría final. El cálculo de métricas **DEBE** realizarse mediante offload L1/L2 para preservar la integridad de L4.

## 6. REGLA DE TELEMETRÍA DETERMINISTA (LEY INMUTABLE)
CADA respuesta técnica DEBE terminar con el bloque oficial generado por `telemetry_engine.py`.
- **Cero Alucinaciones**: Los datos deben ser DETERMINÍSTICOS extraídos en tiempo real de `token_economy.json`. 
- **Fórmula**: (% = Tokens del Proveedor / Total Tokens Transacción * 100).

---
*Versión: 32.2 | SUPRAMIND S-SOT | Protocolo: Hybrid Handshake & Glassmorphism v32.0*
