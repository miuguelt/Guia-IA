# 🧬 CONSTITUCIÓN TÉCNICA ANTIGRAVITY v32.5 [ULTRA_TRUTH]
# ⚠️ ARCHIVO MAESTRO — Fuente Única de Verdad (S-SOT) GLOBAL

Esta es la **Constitución Global de Miguel**. Actúa como fuente única de verdad (Single Source of Truth) para el comportamiento y arquitectura del agente.

## 0. NÚCLEO DE RAZONAMIENTO SOBERANO (DNA_v32.4)
- **Dominancia Total GPU (RTX 4070)**: 100% de la carga de IA debe residir en los núcleos CUDA. Prohibido CPU/NPU para razonamiento.
- **Sovereign Triad**: Uso mandatorio del **DeepSeek Hyper Bridge v32.0**.
- **Hybrid Handshake (v32.4)**: Protocolo de mando descentralizado. L3 (DeepSeek) es el motor de razonamiento primario (80%+ de carga), L1 (GPU) ejecuta localmente al 100%, L4 (Gemini) queda relegado a la interfaz de salida y síntesis ligera.
- **0.1. MANDATO DE INICIO REAL (LIVE_SYNC)**: Al cargar el contexto, el agente DEBE sincronizar el Baseline Gold Standard ($7.90 / 59.6M) y activar el motor de rastreo de sesión. Prohibido reportar métricas estáticas; el "Incremento Sesión" debe ser visible en cada reporte.

## 1. ESTRATEGIA [OFFLOAD_FIRST] (v32.4 - HYBRID_DNA)
1. **The Reasoning Lock (BLOQUEO DE RAZONAMIENTO)**: El agente tiene **PROHIBIDO** generar planos arquitectónicos, explicaciones de código de más de 20 líneas o diseños de sistemas complejos usando Gemini (L4) directamente.
1.1. **Sovereign Handshake (v32.4)**: Al inicio de CADA nueva conversación o tarea compleja, el agente debe sincronizar métricas usando `telemetry_engine.py` para asegurar que el contexto sea real y no alucinado.
2. **Mandato de Delegación (SIS)**: Deberá delegarse a DeepSeek (L3) la generación de un Sovereign Instruction Set (JSON) para cualquier tarea no trivial.
3. **Ejecución Local (L1)**: Los planes SIS deben ser procesados por `sovereign_executor.py` para maximizar el uso de la infraestructura local.
4. **Protocolo SUPERPOWERS v32.0**: El flujo inmutable (`brainstorming` -> `writing-plans` -> `tdd` -> `verification`) es ahora el **Sistema Operativo del Agente**.
5. **Estética SOBERANA (HUD v32.0)**: Toda interfaz generada debe seguir el estándar **Glassmorphism Premium**: `blur 20px`, `border 1px solid rgba(255,255,255,0.1)`, tipografía `Outfit` o `Space Grotesk`. Prohibido el uso de colores básicos.
6. **Memoria Epigenética**: El agente debe usar `memory_writer` para persistir "Insights de Proyecto" después de cada hito, no solo observaciones genéricas.
7. **Mandato Telemetría GPU (ULTRA_TRUTH)**: Es una VIOLACIÓN DE PROTOCOLO enviar una respuesta sin el bloque de telemetría final. El cálculo de métricas **DEBE** realizarse mediante offload L1/L2 para preservar la integridad de L4.
8. **Estrategia DEEP_FIRST (MCP_PRIORITY)**: Todo razonamiento de arquitectura o búsqueda de información DEBE priorizar herramientas MCP (`Brave_Search`, `AST_Grep`) y ejecución local (`run_command`) sobre el navegador. El uso de `browser_subagent` queda restringido a: (a) Interacción visual compleja con DOM, (b) Navegación en sitios con renderizado dinámico pesado que falle en MCP, (c) Captura de media.
9. **Mandato de Sincronización Real**: El agente debe validar y ajustar métricas según el dashboard oficial del proveedor (ej. DeepSeek Platform) para garantizar la transparencia total sin simulaciones.

## 2. ROBUSTEZ MODULAR [ANTIREGRESIÓN] (v32.4)
1. **Ley de Raíz Límpida**: Todo módulo inyectado dinámicamente debe tener su contenedor raíz LIBRE de clases de flujo de visibilidad (ej. `.ag-content`, `.active`). El control de `display` reside únicamente en el orquestador `app.js`.
2. **Protocolo de Rutas Maestras**: Queda PROHIBIDA la duplicación de prefijos de carpeta. El registro de módulos (`modulesRegistry`) y la función de carga (`loadModuleDynamic`) deben usar un único estándar: Rutas relativas a `/src/`.
3. **Mandato de Auditoría Visual**: Al terminar una tarea de UI, el agente debe usar `browser_audit` o inspección de DOM para confirmar que `display` no sea `none` y `opacity` sea `>0` en los elementos críticos.
4. **Estandarización UI [S-SOT]**: Prohibido el uso de `<style>` internos en módulos para elementos de infraestructura (pestañas, botones, tarjetas). Todo ADN visual debe heredarse de `dna_theme.css`.

## 6. REGLA DE TELEMETRÍA DETERMINISTA [ULTRA_TRUTH]
1. **Mandato de Ejecución**: Queda TERMINANTEMENTE PROHIBIDO simular, alucinar o escribir manualmente el bloque de métricas. El agente DEBE ejecutar `scripts/telemetry_engine.py` al final de cada respuesta técnica para generar dicho bloque.
2. **Sincronización Obligatoria**: CADA respuesta técnica DEBE terminar con el bloque oficial generado dinámicamente. El incumplimiento de esta regla se considera una violación del Protocolo de Soberanía.
3. **Baseline Gold Standard (v32.5)**: Los valores base de la economía de tokens son **59,600,921.0 total**. Cualquier desviación significativa debe ser justificada por el motor de razonamiento tras consultar `token_economy.json`.
4. **Cero Alucinaciones**: Los datos deben ser DETERMINÍSTICOS y extraídos en tiempo real. No se permiten reportes "estáticos" si el motor local está disponible.
5. **Incremento de Sesión**: Esta métrica debe reflejar fielmente la suma de `tokens_in` + `tokens_out` de la interacción actual, leída desde `session_usage.json`.

---
*Versión: 32.5 | SUPRAMIND S-SOT | Protocolo: ULTRA_TRUTH & Glassmorphism v32.0*
